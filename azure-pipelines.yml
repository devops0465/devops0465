# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool: Devops


steps:

- script: |
    # Replace variables in script.sh
    sed -i "s|\$name|$(name)|g" ./script.sh

- script: |
    bash ./script.sh